import { TypeOfSeatsPipe } from './type-of-seats.pipe';

describe('TypoOfSeatsPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeOfSeatsPipe();
    expect(pipe).toBeTruthy();
  });
});
